<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <bean id="customAccessDenied" class="com.yj.security.CustomAccessDeniedHandler"/>
    <bean id="customLoginSuccess" class="com.yj.security.CustomLoginSuccessHandler"/>

    <!-- bean for not hashing password -->
<!--    <bean id="customPasswordEncoder" class="com.yj.security.CustomNoOpsPasswordEncoder"/>-->

    <!-- bean for hashing password -->
    <bean id="bcryptPasswordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <security:http>
        <security:intercept-url pattern="/auth/all" access="permitAll"/>
        <security:intercept-url pattern="/auth/member" access="hasRole('ROLE_MEMBER')"/>
        <security:intercept-url pattern="/auth/admin" access="hasRole('ROLE_ADMIN')"/>

<!--        <security:access-denied-handler error-page="/accessError"/>-->
        <security:access-denied-handler ref="customAccessDenied"/>

        <!--        <security:form-login login-page="/customLogin"/>-->
        <security:form-login login-page="/customLogin" authentication-success-handler-ref="customLoginSuccess"/>

        <security:logout logout-url="/customLogout" invalidate-session="true"/>
    </security:http>

    <!-- login user config -->
    <security:authentication-manager>
        <security:authentication-provider>

            <security:jdbc-user-service data-source-ref="dataSource"
                                        users-by-username-query="select user_id, user_pw, enabled from member where user_id = ?"
                                        authorities-by-username-query="select user_id, auth from member_auth where user_id = ?"/>
            <security:password-encoder ref="bcryptPasswordEncoder"/>
            <!-- 스프링 시큐리티에서 지정한 테이블 이용 -->
<!--            <security:jdbc-user-service data-source-ref="dataSource"/>-->
<!--            <security:password-encoder ref="customPasswordEncoder"/>-->

            <!-- InMemoryDetailsManager 이용 -->
<!--            <security:user-service>-->
<!--                <security:user name="member" password="{noop}member" authorities="ROLE_MEMBER"/>-->
<!--                <security:user name="admin" password="{noop}admin" authorities="ROLE_MEMBER, ROLE_ADMIN"/>-->
<!--            </security:user-service>-->
        </security:authentication-provider>
    </security:authentication-manager>

</beans>